#include <stdio.h>
#include <stdlib.h>
#include "cons.h"

void add(int memoria[],int a, int b, int atype, int btype){
    if (atype==REG){
        if (btype==INST){
            memoria[a];
        }

    }

}